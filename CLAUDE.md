# CLAUDE.md — 웹사이트 디자인 작업 가이드

이 저장소에서 웹사이트/랜딩페이지/와이어프레임 제작을 요청받으면 아래 원칙을 따른다.
`/website-design` 스킬(.claude/skills/website-design/SKILL.md)과 함께 사용한다.

## 작업 방식

1. **요청 읽기가 먼저다.** 참고 이미지가 있으면 레이아웃·타이포·간격·컴포넌트 구성을 분석해 그대로 재현한다.
   컨셉만 주어지면(예: "북카페 랜딩, 미니멀 흑백") 적절한 섹션 구성을 스스로 설계한다.
2. **코드를 쓰기 전에 디자인 플랜을 먼저 세운다.**
   - Color: 이름 붙인 4–6개 hex 값으로 팔레트 정의
   - Type: 디스플레이용 + 본문용 + (필요시) 캡션/데이터용 서체 페어링
   - Layout: 레이아웃 컨셉을 1–2문장으로
3. 플랜이 "아무 사이트에나 낼 법한 기본값"처럼 읽히면 그 부분을 수정한 뒤에 코드를 쓴다.
4. 결과물은 단일 HTML 파일(HTML/CSS/JS 인라인)로 만들어 브라우저에서 바로 열리게 한다.

## 디자인 원칙 (Fable 수준의 기준선)

- **주제에 뿌리내리기**: 주제의 세계(재료, 도구, 어휘)에서 구체적 선택을 끌어낸다. lorem ipsum 금지, 실제 콘텐츠로 채운다.
- **타이포그래피가 페이지를 끌고 간다**: 타입 스케일을 정하고 지킨다. 본문 폭은 65자 내외, 제목엔 `text-wrap: balance`, 대문자 라벨엔 letter-spacing.
- **회색도 선택이다**: 순수 mid-grey는 무성의하게 읽힌다. 액센트 색 쪽으로 살짝 기운 회색을 쓴다.
- **라이트/다크 두 테마 모두 설계**: 팔레트를 `:root` 커스텀 프로퍼티(토큰)로 정의하고, `@media (prefers-color-scheme: dark)`와 `:root[data-theme="dark"]`/`[data-theme="light"]`에서 토큰만 재정의한다. 컴포넌트는 항상 토큰을 통해 스타일링한다.
- **간격은 레이아웃이 담당**: 형제 요소 간격은 flex/grid + `gap`으로. 개별 margin 남발 금지. 넓은 콘텐츠(표·코드)는 자체 컨테이너에 `overflow-x: auto`.
- **숫자 정렬**: 숫자가 세로로 정렬되는 곳엔 `font-variant-numeric: tabular-nums`.
- **AI 티 나는 디자인 회피**: 크림색 배경 + 세리프 + 테라코타, 검정 배경 + 형광 초록 하나, 보라→파랑 그라데이션 히어로, Inter/Space Grotesk 무지성 선택, 이모지 섹션 마커, 전부 가운데 정렬, 모든 것에 rounded-lg — 사용자가 명시적으로 원하지 않는 한 이 기본값들에 자유를 낭비하지 않는다.
- **카피도 디자인 재료**: 사용자 관점의 언어, 능동태, 버튼은 일어날 일을 정확히 말한다("게시하기" → "게시됨").
- **구조 장치는 정보여야 한다**: 01/02/03 번호 매기기는 내용이 실제 순서일 때만. 장식용 구조물 금지.
- **대담함은 한 곳에만**: 과감한 선택은 한 군데에 집중하고 주변은 조용하게 유지한다.

## 품질 체크

- 요소 겹침, CSS 특이도 충돌(`.section` vs `.cta`가 서로 패딩을 상쇄하는 류), 폰트 폴백 누락 확인
- 키보드 포커스 상태 표시, `prefers-reduced-motion` 존중
- 페이지 본문이 가로 스크롤되면 안 됨 (넓은 요소는 자체 스크롤 컨테이너로)
